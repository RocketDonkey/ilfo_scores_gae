IlFo Score Scraper (GAE Version)

Basic code for the IlFo Score Scraper (needs a new name). Based on information provided in the config file, the app scrapes the provided sites for each restaurant (daily) and writes the values to the datastore. The results on the main page show that day's scores.
